# Necessarias para db.py, api.py, avaliacao.py e otimizar.py
fastapi==0.141.1
uvicorn==0.52.4
dspy==3.3.1

# Necessarias para rodar o bot.py completo (bot do Telegram + transcricao de voz).
# openai-whisper e pesado (puxa PyTorch) e tambem precisa do ffmpeg instalado
# no sistema (nao e um pacote Python, instale via apt/brew/choco).
pyTelegramBotAPI==4.36.1
openai-whisper==20250625
